v133.txt

[v133]
  1. Файлсеты:
    +1. Возможность задавать имя для файлсета:
      + имя файлсета
      + description
      + [x] - загружать при старте
    +2. Корректное удаление файлсетов;
    3. Сохранение конфигурации файлсета;
    4. Загрузка конфигурации файлсета;
    +5. Кнопки группового выделения файлов: +, -, *
    6. Групповые операции над файлами
      + Мультиселект файлов в файлсете;
      - Коллективный check/uncheck
      - Задание кодировки для выбранных файлов
      - Коллективное удаление файла из файлсета
    7. Возможность задания токена для таймштапа;
    8. Сканировать каталог в список файлов;
  2. SearchView:
    1. Фильтрация по диапазону времени
    2. Возможность задавать имя для SearchView;
  3. Моноширинный шрифт в SearchResult;
  4. Сохранять конфигурацию SearchView в конфиг-файлы (без результатов поиска):
    4.1. диапазон времени поиска (двухсторонний Slider);
    4.2. Моноширинный шрифт для Search View;
  5. окно About
  6. Механизм поиска:
    1. Отладка токенайзера:
      - if STRING NOT OPEN_BR => STRING AND NOT OPEN_BR
      - if CLOSE_BR NOT OPEN_BR => CLOSE_BR AND NOT OPEN_BR
    2. Отладка парсера по корректным сценариям без скобок
	  - val1 AND val2 OR val3 OR val4
	  - val1 AND val2 OR val3 AND val4
    3. Отладка парсера по корректным сценариям со скобками
      - val1 AND NOT (val2 OR val3)

[v134]
  1. Command Line перевести на новый токенайзер и парсер
  2. Распараллеливать работу по поиску
  3. Отмена начатого поиска

-------------------------------

[v133]
  1. Файлсеты:
    1. Коллективное задание кодировки для файлов;
    2. Возможность задавать имя для файлсета;
    3. Сохранение конфигурации файлсета;
    4. Загрузка файлсета;
    5. Сканировать каталог в список файлов;
    6. Корректное удаление файлсетов;
    7. Удаление файлов из файлсетов;
  2. SearchView
    1. Возможность задавать имя для SearchView;
  3. Моноширинный шрифт в SearchResult
  4. Сохранять конфигурацию SearchView в конфиг-файлы (без результатов поиска)
    6.3. диапазон времени поиска (двухсторонний Slider);
    6.4. Моноширинный шрифт для Search View;
  5. окно About
  6. Механизм поиска:
	  2. Отладка токенайзера:
		- if CLOSE_BR NOT OPEN_BR => CLOSE_BR AND NOT OPEN_BR
	  3. Отладка парсера по корректным сценариям без скобок
		- val1 AND val2 OR val3 OR val4
		- val1 AND val2 OR val3 AND val4
	    - 4.2. val1 AND NOT (val2 OR val3)

[v134]
  1. Command Line перевести на новый токенайзер и парсер
  2. Распараллеливать работу по поиску
  3. Отмена начатого поиска

  
Разработка регулярного выражения
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
[Вариант 1]
Разбивает на слова и скобки
[\w]+|\(|\)

wolf AND (fox OR deer) -> wolf, AND, (, fox, OR, deer, )
wolf AND "red fox" -> wolf, AND, red, fox


[Вариант 2]
Выбирает слова и то, что между кавычками, но в выборку попадает целиком выражение "red fox" OR "brown deer",
а нужно, чтобы попадало отдельно "red fox" и "brown deer"
\".+\"|[\w]+|\(|\)
\".*\"|\w+|\(|\)
wolf AND "red fox" OR "brown deer"


[Вариант 3]
Символ " заменяем на подстроку &qt
&qt.+&qt{1}?|\w+
wolf AND &qtred fox&qt OR &qtbrown deer&qt


[Вариант 4]
Работающее выражение
\".+?\"|\w+|\(|\)
wolf AND "red fox" OR "brown deer" AND (frog OR lizard)


!!! Варианты синтаксиса: 
  1. AND, OR, NOT => 
  ! Для линуксовой локали не берет русский текст в качестве \w
  ! Внимание: знак '=' не обрабатывает!!!
    regexp: \".+?\"|\w+|\(|\) query: wolf AND "red fox" AND NOT "brown deer" AND (frog OR NOT lizard)
	regexp: \".+?\"|\(|\)|[\w\[\]\\\/\^\$\.\|\?\*\+\{\}<>]+
  regexp: \".+?\"|\(|\)|[\w\[\]\\\/\^\$\.\|\?\*\+\{\}<>!@#%_-]+
  
  2. _AND_, _OR_, _NOT_  => regexp: 
  3. &&, ||, ! => regexp: !?\s*\".+?\"|!?\s*\w+|!?\s*\(|\)|&&|\|\| query: wolf && "red fox" && ! "brown deer" && (frog || !lizard)
    !? -> допускается 0 или 1 символ ! перед выражением с кавычками
	\s* -> допускается наличие пробела(ов) между ! и выражением с кавычками
	\".+?\"| -> выражение с кавычками
	!? -> допускается символ ! перед словом
	\s*
	\w+| -> слово
	\(|\)| -> Открывающаяся и закрывающаяся скобка
	&&| ->
	\|\| ->

Таблица переходов для логического парсера
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
